# User Development Environment

⚠️ **IMPORTANT — read this before giving any shell/build instructions!**

## Operating System
- **macOS** (NOT Windows!)
- Use **Terminal / zsh / bash**, NOT PowerShell
- Use **forward slashes** `/` in paths, NOT backslashes
- Use `./scripts/build-ios.sh` (the `.sh` version), NOT `.ps1`
- Use `~/` for home directory

## Local repo path
- **Project path**: `~/dev/easli`
- Use this exact path in any `cd` commands
  Example: `cd ~/dev/easli && git pull origin main`

## GitHub
- **Username**: `mikio-inked`
- Repo: `github.com/mikio-inked/easli` (assumed — confirm if different)

## Build Pipeline
- Local builds via **EAS CLI** on Mac
- iOS builds: `./scripts/build-ios.sh`
- Android builds: `./scripts/build-android.sh production` (AAB) or `production-apk` (APK)

## Communication
- Respond in **German** (user's preferred language)
- User's name: Martin Tran
- Apple ID for App Store Connect: `m.tran@icloud.com`

